import Logo from "../Logo/Logo";

/**
 * Header component
 *
 * @todo add styles
 * @returns header component
 */
const Header = () => {
  return (
    <header>
      <Logo />
    </header>
  );
};

export default Header;
