/**
 * Counter component
 *
 * @todo add styles
 * @todo call bff to get the counter value
 * @todo make the number humanly readable
 * @returns
 */
const Counter = () => {
  return <div>Counter</div>;
};

export default Counter;
